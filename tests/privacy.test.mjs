import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import test from"node:test";
test("browser bundle has no path selector, applicant answers, credentials, or business mutations",async()=>{const s=await readFile("src/plugin.jsx","utf8");for(const forbidden of ["password","cookie","accessToken","applicantId","digest","filename","checkpoint","evidence","filesystem.read","studylink.save","studylink.submit"])assert.equal(s.includes(forbidden),false,forbidden);assert.match(s,/unsupported here and were not executed/i);assert.match(s,/DETERMINISTIC SYNTHETIC DEMO/)});
